import { StaticArrayPipe } from './static-array.pipe';

describe('StaticArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new StaticArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
