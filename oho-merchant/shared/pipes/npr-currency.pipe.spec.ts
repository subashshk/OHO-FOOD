import { NprCurrencyPipe } from './npr-currency.pipe';

describe('NprCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new NprCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
