import { RemovePlusCodePipe } from './remove-plus-code.pipe';

describe('RemovePlusCodePipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePlusCodePipe();
    expect(pipe).toBeTruthy();
  });
});
